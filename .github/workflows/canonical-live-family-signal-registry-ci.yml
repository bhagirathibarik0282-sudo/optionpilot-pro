name: Canonical Live Family Signal Registry CI

on:
  pull_request:
    paths:
      - "canonical-live-family-signal-registry.ts"
      - "canonical-normalized-family-support-producer.ts"
      - "test/canonical-live-family-signal-registry.test.ts"
      - ".github/workflows/canonical-live-family-signal-registry-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: >-
          NODE_ENV=test node --import tsx --test
          test/canonical-live-family-signal-registry.test.ts
          test/canonical-normalized-family-support-producer.test.ts
          test/canonical-business-mission-orchestrator.test.ts
