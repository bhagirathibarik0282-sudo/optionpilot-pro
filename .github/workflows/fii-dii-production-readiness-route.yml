name: FII DII Production Readiness Route CI

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  readiness-route:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: node --import tsx --test test/canonical-fii-dii-production-readiness.test.ts
      - run: node --import tsx --test test/canonical-fii-dii-production-readiness-http.test.ts
