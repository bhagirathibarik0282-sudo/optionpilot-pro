name: H1 Exact Live Config Preflight CI

on:
  pull_request:
    paths:
      - "h1-exact-live-config-preflight.ts"
      - "test/h1-exact-live-config-preflight.test.ts"
      - ".github/workflows/h1-exact-live-config-preflight-ci.yml"

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npx tsx --test test/h1-exact-live-config-preflight.test.ts
