name: H1 Premium Pair Divergence CI
on:
  pull_request:
    paths:
      - 'h1-premium-pair-divergence-v1.ts'
      - 'test/h1-premium-pair-divergence-v1.test.ts'
      - '.github/workflows/h1-premium-pair-divergence-ci.yml'
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npx tsx --test test/h1-premium-pair-divergence-v1.test.ts
