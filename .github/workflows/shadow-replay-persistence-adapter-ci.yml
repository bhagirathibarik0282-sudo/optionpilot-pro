name: Shadow Replay Persistence Adapter CI

on:
  pull_request:
    branches: [main, automation-staging]
    paths:
      - 'shadow-replay-persistence-adapter.ts'
      - 'shadow-replay-persistence.ts'
      - 'test/shadow-replay-persistence-adapter.test.ts'
      - '.github/workflows/shadow-replay-persistence-adapter-ci.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: node --import tsx --test test/shadow-replay-persistence-adapter.test.ts
