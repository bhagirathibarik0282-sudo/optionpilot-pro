name: Shadow Startup Recovery Diagnostic Snapshot CI

on:
  pull_request:
    branches: [automation-staging]
    paths:
      - 'shadow-startup-recovery-diagnostic-snapshot.ts'
      - 'test/shadow-startup-recovery-diagnostic-snapshot.test.ts'
      - '.github/workflows/shadow-startup-recovery-diagnostic-snapshot-ci.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: node --import tsx --test test/shadow-startup-recovery-diagnostic-snapshot.test.ts
