name: Shadow Startup Recovery Route Registration Adapter CI

on:
  pull_request:
    branches: [automation-staging]
    paths:
      - 'shadow-startup-recovery-route-registration-adapter.ts'
      - 'test/shadow-startup-recovery-route-registration-adapter.test.ts'
      - '.github/workflows/shadow-startup-recovery-route-registration-adapter-ci.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: node --import tsx --test test/shadow-startup-recovery-route-registration-adapter.test.ts
